# Use `python -m pip install -e .` in virtual env to build
from setuptools import find_packages, setup

setup(
    name='pct',
    packages=find_packages(),
    version='1.0.0',
    description='Source code for PCT',
    author='Felipe&Robbe',
)

